export const users = state => state.users.data;

export const limit = state => state.users.limit;

export const page = state => state.users.page;

export const totalPages = state => state.users.totalPages;

export const sort = state => state.users.sort;
